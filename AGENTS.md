# DryNow 開発ガイド

## プロジェクトの目的

- DryNowは、気象データから洗濯物の外干し可否と乾燥時間を推定するFlutterアプリである。
- 現在はPhase 1として、気象APIの比較とデータ収集基盤を検証している。
- Flutter本体は`lib/`、比較・収集用Pythonツールは`tools/api_comparison/`にある。
- 現時点のFlutter画面はOpenWeatherの現在値を確認する検証用の構成である。
- 外干し判定や乾燥時間推定は、まだ実装済みとみなさない。

## 最初に確認する資料とコード

- プロジェクト概要とロードマップは`README.md`を確認する。
- 気象データ検証の経緯は`docs/WEATHER_DATA_VALIDATION.md`を確認する。
- API比較基盤の現行方針は`docs/DRYNOW_API_COMPARISON_DESIGN_v1.1.md`を確認する。v1.0は当初設計の履歴として参照する。
- Pythonツール固有の説明は`tools/api_comparison/README.md`を確認する。
- Flutter変更時は`lib/main.dart`、`lib/weather_api.dart`、`lib/models/weather.dart`を確認する。
- Python変更時は対象collectorに加え、`collect_current.py`、`database.py`、`locations.py`も確認する。
- READMEだけで判断せず、変更対象とその呼び出し元・保存先まで読む。
- 実装とドキュメントが異なる場合は、実コードを確認してズレを報告する。

## 変更前と変更中のルール

- 不明な仕様を推測で確定せず、影響が大きい場合はユーザーへ質問する。
- 一度に大規模な変更を行わず、目的ごとに小さく変更する。
- 既存機能、保存済みデータ、ユーザーの未コミット変更を不用意に壊さない。
- 関係のないリファクタリングや依存関係更新を同じ変更へ混ぜない。
- 既存ファイルは、明確な理由と指示がない限り改名・移動しない。
- Flutterが自動生成した各プラットフォームのファイルは原則変更しない。

## 気象データのルール

- 実測値の`0`と欠損値の`NULL`を明確に区別する。
- APIから取得できなかった値を推測で`0`へ変換しない。
- APIエラーと、正常取得した結果が`0`だった場合を区別する。
- raw JSONを失わず、正規化後の値とは別に扱う。
- 雨、雪、雨雪混在、項目欠損などAPIレスポンスの違いを考慮する。
- API固有の単位や降水の集計時間を確認し、無理に同じ意味へ変換しない。
- `target_time`、`fetched_at`、`source_time`の意味を混同しない。
- UTC、JST、API地点のタイムゾーンを確認して日時を扱う。
- SQLiteの一意制約を確認し、同一対象時刻での再実行動作を明確にする。
- 成功データだけでなく、失敗や欠損を評価できる情報も保持する。
- APIキーを含むURLは保存せず、`endpoint_sanitized`では必ず秘密部分を隠す。

## コメントと命名

- 新しく書くコード内のコメントは日本語にする。
- コメントは判断理由や注意点が必要な箇所だけに簡潔に書く。
- コードを読めば分かる処理を過剰に説明しない。
- ファイル名とフォルダ名は、短く役割が分かる英語名にする。
- 意味の薄い略称や、必要以上に長い名前を避ける。
- 既存コードの命名規則と、Dart・Pythonそれぞれの慣習を尊重する。

## セキュリティ

- APIキー、トークン、認証情報をコードへ直接書かない。
- `.env`などの秘密情報をコミット対象にしない。
- ログ、テスト出力、作業報告、rawデータに秘密情報を表示しない。
- `.env.example`には変数名と安全な例だけを記載する。
- Flutterアセットへ含めた値は秘密として保護できないことを考慮する。

## Gitのルール

- ユーザーから明示的な指示がない限り、コミットやプッシュを行わない。
- コミットメッセージを求められた場合は、日本語で簡潔に書く。
- ユーザーから「コミットして」「ここまでコミットして」などと指示された場合も、すぐに`git commit`を実行しない。
- コミット前に変更内容を確認し、日本語のコミットメッセージ案を1つ提示して、「このメッセージでコミットしてよいか」をユーザーへ確認する。
- ユーザーがコミットメッセージを明示的に承認するまで、`git commit`を実行しない。
- ユーザーからコミットメッセージの修正を求められた場合は、修正版を提示し、改めて承認を確認する。
- コミット前に変更内容、確認結果、残っている問題を報告する。
- 関係のない既存変更を修正、削除、ステージしない。
- 作業後は`git diff`と`git status --short`で変更範囲を確認する。
- 改行差分や生成ファイルの変更表示は、実内容を確認してから判断する。
- DB、ログ、`.env`など`.gitignore`対象を誤って追加しない。

## 動作確認

- Flutterを変更した場合は、原則として`flutter analyze`を実行する。
- Dartのテストを追加・変更した場合は、関連する`flutter test`を実行する。
- Pythonを変更した場合は、関連テストまたは外部更新を伴わない安全な確認を実行する。
- API通信やDB書き込みを伴う確認は、対象と副作用を確認してから行う。
- 実行できなかった確認は、理由と未確認範囲を隠さず報告する。
- テストを通すためだけに、無関係なコードや期待値を変更しない。

## 作業後の報告

- 報告は日本語で簡潔に行う。
- 変更したファイル、変更内容、変更理由を記載する。
- 実行した確認と結果を記載する。
- 未解決の問題、未実行の確認、注意点があれば明記する。
- 変更内容を報告するときは、重要な処理と変更理由をユーザーが理解できるよう簡潔に説明する。
